export enum UserState {
    ONLINE = "online",
    OFFLINE = "offline",
  }
  
  export enum FeedType {
    RANDOM = "random",
    CHRONOLOGICAL = "chronological",
    SUCCESS_OF_POST = "success_of_post",
    SUCCESS_OF_WRITER = "success_of_writer",
    SIMILARITY_SENDER = "similarity_sender",
    SIMILARITY_MESSAGE = "similarity_message",
    TONE = "tone",
    NOT_CONSUMED = "not_consumed",
  }
  
  export enum ActionType {
    CREATE_POST = "create_post",
    COMMENT = "comment",
    LIKE = "like",
    SHARE = "share",
  }
  
  